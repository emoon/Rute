
struct PlatformClipboard {
    [replace] mime_data(mode: Clipboard::Mode) -> MimeData?,
    [replace] set_mime_data(data: *MimeDataType, mode: Clipboard::Mode),
    [replace] supports_mode(mode: Clipboard::Mode) -> bool,
    [replace] owns_mode(mode: Clipboard::Mode) -> bool,
    emit_changed(mode: Clipboard::Mode),
}

// vim: syntax=rust expandtab ts=4 sw=4
