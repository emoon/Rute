#[org_name(QMutex)]
enum RecursionMode {
    NonRecursive,
    Recursive,
}

struct Mutex : BasicMutex {
    lock(),
    unlock(),
    is_recursive() -> bool,
}

// vim: syntax=rust expandtab ts=4 sw=4
