
struct DockAreaLayout {
    is_valid() -> bool,
    save_state(&DataStreamType stream),
    restore_state(&DataStreamType stream, &List<QDockWidgetType widgets, bool testing) -> bool,
    index_of_place_holder(String object_name) -> <i32>,
    index_of(&WidgetType dock_widget) -> <i32>,
    gap_index(&PointType pos, bool disallow_tabs) -> <i32>,
    find_separator(&PointType pos) -> <i32>,
    item(&List<int>Type path) -> &DockAreaLayoutItemType,
    info(&List<int>Type path) -> &DockAreaLayoutInfoType,
    info(&List<int>Type path) -> &DockAreaLayoutInfoType,
    info(&WidgetType widget) -> &DockAreaLayoutInfoType,
    item_rect(&List<int>Type path) -> RectType,
    separator_rect(i32 index) -> RectType,
    separator_rect(&List<int>Type path) -> RectType,
    insert_gap(&List<int>Type path, &LayoutItemType dock_widget_item) -> bool,
    plug(&List<int>Type path) -> &LayoutItemType,
    unplug(&List<int>Type path) -> &LayoutItemType,
    remove(&List<int>Type path),
    remove_place_holder(String name),
    fit_layout(),
    clear(),
    size_hint() -> SizeType,
    minimum_size() -> SizeType,
    add_dock_widget(Internal::DockPosition pos, &DockWidgetType dock_widget, Rute::Orientation orientation),
    restore_dock_widget(&DockWidgetType dock_widget) -> bool,
    split_dock_widget(&DockWidgetType after, &DockWidgetType dock_widget, Rute::Orientation orientation),
    tabify_dock_widget(&DockWidgetType first, &DockWidgetType second),
    resize_docks(&List<QDockWidgetType docks, &List<int>Type sizes, Rute::Orientation o),
    apply(bool animate),
    paint_separators(&i32 p, &WidgetType widget, &RegionType clip, &PointType mouse),
    separator_region() -> RegionType,
    separator_move(&List<int>Type separator, &PointType origin, &PointType dest) -> i32,
    update_separator_widgets(),
    item_at(&i32 x, i32 index) -> &LayoutItemType,
    take_at(&i32 x, i32 index) -> &LayoutItemType,
    delete_all_layout_items(),
    get_grid(&Vector<QLayoutStruct>Type ver_struct_list, &Vector<QLayoutStruct>Type hor_struct_list),
    set_grid(&Vector<QLayoutStruct>Type ver_struct_list, &Vector<QLayoutStruct>Type hor_struct_list),
    gap_rect(&List<int>Type path) -> RectType,
    keep_size(&DockWidgetType w),
    used_tab_bars() -> &Set<QTabBarType,
    used_separator_widgets() -> &Set<QWidgetType,
    style_changed_event(),
}

