
struct GraphicsSceneBspTree {
    initialize(&RectFType rect, i32 depth),
    clear(),
    insert_item(&GraphicsItemType item, &RectFType rect),
    remove_item(&GraphicsItemType item, &RectFType rect),
    remove_items(&Set<QGraphicsItemType items),
    items(&RectFType rect, bool only_top_level_items) -> <&GraphicsItemType>,
    leaf_count() -> i32,
    first_child_index(i32 index) -> i32,
    parent_index(i32 index) -> i32,
    debug(i32 index) -> String,
    initialize(&RectFType rect, i32 depth, i32 index),
    climb_tree(&GraphicsSceneBspTreeVisitorType visitor, &RectFType rect, i32 index),
    rect_for_index(i32 index) -> RectFType,
}

// vim: syntax=rust expandtab ts=4 sw=4
