struct IconModeViewBase : CommonListViewBase {
    item_index(&i32 item) -> i32,
    index_to_list_view_item(&i32 index) -> i32,
    do_batched_item_layout(&ListViewLayoutInfoType info, i32 max) -> bool,
    clear(),
    set_row_count(i32 row_count),
    scroll_contents_by(i32 dx, i32 dy, bool scroll_elastic_band),
    data_changed(&i32 top_left, &i32 bottom_right),
    append_hidden_row(i32 row),
    remove_hidden_row(i32 row),
    set_position_for_index(&i32 position, &i32 index),
    filter_drag_move_event(&i32 arg0) -> bool,
    filter_drag_leave_event(&i32 arg0) -> bool,
    filter_drop_event(&i32 e) -> bool,
    filter_start_drag(i32 arg0) -> bool,
    init_bsp_tree(&i32 contents),
    init_dynamic_layout(&ListViewLayoutInfoType info) -> i32,
    do_dynamic_layout(&ListViewLayoutInfoType info),
    add_leaf(i32 arg0),
    items_rect(i32 arg0) -> i32,
    dragged_items_rect() -> i32,
    snap_to_grid(&i32 pos) -> i32,
    update_contents_size(),
    dragged_items_delta() -> i32,
    draw_items(&i32 painter, i32 arg0),
    move_item(i32 index, &i32 dest),
}

