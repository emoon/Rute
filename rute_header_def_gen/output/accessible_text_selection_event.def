struct AccessibleTextSelectionEvent : AccessibleTextCursorEvent {
    set_selection(i32 start, i32 end),
    selection_start() -> i32,
    selection_end() -> i32,
}

// vim: syntax=rust expandtab ts=4 sw=4
