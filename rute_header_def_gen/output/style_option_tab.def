enum StyleOptionType {
    Type,
}

enum StyleOptionVersion {
    Version,
}

enum TabPosition {
    Beginning,
    Middle,
    End,
    OnlyOneTab,
}

enum SelectedPosition {
    NotAdjacent,
    NextIsSelected,
    PreviousIsSelected,
}

enum CornerWidget {
    NoCornerWidgets,
    LeftCornerWidget,
    RightCornerWidget,
}

enum TabFeature {
    None,
    HasFrame,
}

