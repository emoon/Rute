
struct BasicMutex {
    lock(),
    unlock(),
    ///     \since 5.7
    /// 
    ///     Returns \c true if the mutex is recursive.
    is_recursive() -> bool,
    ///     \since 5.7
    /// 
    ///     Returns \c true if the mutex is recursive.
    is_recursive() -> bool,
}

// vim: syntax=rust expandtab ts=4 sw=4
