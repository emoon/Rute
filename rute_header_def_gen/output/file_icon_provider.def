enum IconType {
    Computer,
    Desktop,
    Trashcan,
    Network,
    Drive,
    Folder,
    File,
}

enum Option {
    DontUseCustomDirectoryIcons,
}


struct FileIconProvider {
    [override] icon(type: FileIconProvider::IconType) -> Icon,
    [override] icon(info: &FileInfoType) -> Icon,
    [override] type(info: &FileInfoType) -> String,
    set_options(options: Options),
    options() -> Options,
}

// vim: syntax=rust expandtab ts=4 sw=4
