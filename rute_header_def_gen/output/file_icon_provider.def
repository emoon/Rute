
struct FileIconProvider {
    icon(FileIconProvider::IconType type) -> IconType,
    icon(&FileInfoType info) -> IconType,
    type(&FileInfoType info) -> String,
    set_options(Options options),
    options() -> Options,
}

