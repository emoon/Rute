struct HeaderView : AbstractItemView {
    set_model(&AbstractItemModelType model),
    orientation() -> Rute::Orientation,
    offset() -> i32,
    length() -> i32,
    size_hint() -> SizeType,
    set_visible(bool v),
    section_size_hint(i32 logical_index) -> i32,
    visual_index_at(i32 position) -> i32,
    logical_index_at(i32 position) -> i32,
    logical_index_at(i32 x, i32 y) -> i32,
    logical_index_at(&PointType pos) -> i32,
    section_size(i32 logical_index) -> i32,
    section_position(i32 logical_index) -> i32,
    section_viewport_position(i32 logical_index) -> i32,
    move_section(i32 from, i32 to),
    swap_sections(i32 first, i32 second),
    resize_section(i32 logical_index, i32 size),
    resize_sections(HeaderView::ResizeMode mode),
    is_section_hidden(i32 logical_index) -> bool,
    set_section_hidden(i32 logical_index, bool hide),
    hidden_section_count() -> i32,
    hide_section(i32 logical_index),
    show_section(i32 logical_index),
    count() -> i32,
    visual_index(i32 logical_index) -> i32,
    logical_index(i32 visual_index) -> i32,
    set_sections_movable(bool movable),
    sections_movable() -> bool,
    set_sections_clickable(bool clickable),
    sections_clickable() -> bool,
    set_highlight_sections(bool highlight),
    highlight_sections() -> bool,
    section_resize_mode(i32 logical_index) -> HeaderView::ResizeMode,
    set_section_resize_mode(HeaderView::ResizeMode mode),
    set_section_resize_mode(i32 logical_index, HeaderView::ResizeMode mode),
    set_resize_contents_precision(i32 precision),
    resize_contents_precision() -> i32,
    set_sort_indicator_shown(bool show),
    is_sort_indicator_shown() -> bool,
    set_sort_indicator(i32 logical_index, Rute::SortOrder order),
    sort_indicator_section() -> i32,
    sort_indicator_order() -> Rute::SortOrder,
    cascading_section_resizes() -> bool,
    set_cascading_section_resizes(bool enable),
    default_section_size() -> i32,
    set_default_section_size(i32 size),
    reset_default_section_size(),
    minimum_section_size() -> i32,
    set_minimum_section_size(i32 size),
    maximum_section_size() -> i32,
    set_maximum_section_size(i32 size),
    default_alignment() -> Rute::Alignment,
    set_default_alignment(Rute::Alignment alignment),
    do_items_layout(),
    sections_moved() -> bool,
    sections_hidden() -> bool,
    save_state() -> ByteArrayType,
    restore_state(&ByteArrayType state) -> bool,
    reset(),
    set_offset(i32 offset),
    set_offset_to_section_position(i32 visual_index),
    set_offset_to_last_section(),
    header_data_changed(Rute::Orientation orientation, i32 logical_first, i32 logical_last),
    section_moved(i32 logical_index, i32 old_visual_index, i32 new_visual_index),
    section_resized(i32 logical_index, i32 old_size, i32 new_size),
    section_pressed(i32 logical_index),
    section_clicked(i32 logical_index),
    section_entered(i32 logical_index),
    section_double_clicked(i32 logical_index),
    section_count_changed(i32 old_count, i32 new_count),
    section_handle_double_clicked(i32 logical_index),
    sort_indicator_changed(i32 logical_index, Rute::SortOrder order),
    update_section(i32 logical_index),
    resize_sections(),
    sections_inserted(&ModelIndexType parent, i32 logical_first, i32 logical_last),
    sections_about_to_be_removed(&ModelIndexType parent, i32 logical_first, i32 logical_last),
    initialize(),
    initialize_sections(),
    initialize_sections(i32 start, i32 end),
    current_changed(&ModelIndexType current, &ModelIndexType old),
    event(&EventType e) -> bool,
    paint_event(&PaintEventType e),
    mouse_press_event(&MouseEventType e),
    mouse_move_event(&MouseEventType e),
    mouse_release_event(&MouseEventType e),
    mouse_double_click_event(&MouseEventType e),
    viewport_event(&EventType e) -> bool,
    paint_section(&PainterType painter, &RectType rect, i32 logical_index),
    section_size_from_contents(i32 logical_index) -> SizeType,
    horizontal_offset() -> i32,
    vertical_offset() -> i32,
    scroll_contents_by(i32 dx, i32 dy),
    data_changed(&ModelIndexType top_left, &ModelIndexType bottom_right, &Vector<int>Type roles),
    rows_inserted(&ModelIndexType parent, i32 start, i32 end),
    visual_rect(&ModelIndexType index) -> RectType,
    scroll_to(&ModelIndexType index, AbstractItemView::ScrollHint hint),
    index_at(&PointType p) -> ModelIndexType,
    is_index_hidden(&ModelIndexType index) -> bool,
    move_cursor(AbstractItemView::CursorAction arg0, Rute::KeyboardModifiers arg1) -> ModelIndexType,
    set_selection(&RectType rect, ItemSelectionModel::SelectionFlags flags),
    visual_region_for_selection(&ItemSelectionType selection) -> RegionType,
    init_style_option(&StyleOptionHeaderType option),
}

