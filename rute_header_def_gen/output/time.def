
struct Time {
    is_null() -> bool,
    is_valid() -> bool,
    hour() -> i32,
    minute() -> i32,
    second() -> i32,
    msec() -> i32,
    set_hms(i32 h, i32 m, i32 s, i32 ms) -> bool,
    add_secs(i32 secs) -> TimeType,
    secs_to(&TimeType arg0) -> i32,
    add_m_secs(i32 ms) -> TimeType,
    msecs_to(&TimeType arg0) -> i32,
    from_m_secs_since_start_of_day(i32 msecs) -> TimeType,
    msecs_since_start_of_day() -> i32,
    current_time() -> TimeType,
    is_valid(i32 h, i32 m, i32 s, i32 ms) -> bool,
    start(),
    restart() -> i32,
    elapsed() -> i32,
}

// vim: syntax=rust expandtab ts=4 sw=4
