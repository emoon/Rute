enum ResultFlag {
    Ignore,
    MayBeGesture,
    TriggerGesture,
    FinishGesture,
    CancelGesture,
    ResultState_Mask,
    ConsumeEventHint,
    ResultHint_Mask,
}


struct GestureRecognizer {
    [replace] create(target: &ObjectType) -> Gesture?,
    [replace] recognize(state: &GestureType, watched: &ObjectType, event: &EventType) -> Result,
    [replace] reset(state: &GestureType),
    [static] register_recognizer(recognizer: &GestureRecognizerType) -> Rute::GestureType,
    [static] unregister_recognizer(type: Rute::GestureType),
}

// vim: syntax=rust expandtab ts=4 sw=4
