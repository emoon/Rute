
struct CommonListViewBase {
    item_index(&i32 item) -> i32,
    index_to_list_view_item(&i32 index) -> i32,
    do_batched_item_layout(&ListViewLayoutInfoType info, i32 max) -> bool,
    clear(),
    set_row_count(i32 arg0),
    data_changed(&i32 arg0, &i32 arg1),
    horizontal_scroll_to_value(i32 index, i32 hint, bool left_of, bool right_of, &i32 area, &i32 rect) -> i32,
    vertical_scroll_to_value(i32 index, i32 hint, bool above, bool below, &i32 area, &i32 rect) -> i32,
    scroll_contents_by(i32 dx, i32 dy, bool scroll_elastic_band),
    map_to_viewport(&i32 rect) -> i32,
    horizontal_offset() -> i32,
    vertical_offset() -> i32,
    update_horizontal_scroll_bar(&i32 step),
    update_vertical_scroll_bar(&i32 step),
    append_hidden_row(i32 row),
    remove_hidden_row(i32 row),
    set_position_for_index(&i32 arg0, &i32 arg1),
    paint_drag_drop(&i32 painter),
    filter_drag_move_event(&i32 arg0) -> bool,
    filter_drag_leave_event(&i32 arg0) -> bool,
    filter_drop_event(&i32 arg0) -> bool,
    filter_start_drag(i32 arg0) -> bool,
    spacing() -> i32,
    is_wrapping() -> bool,
    grid_size() -> i32,
    flow() -> i32,
    movement() -> i32,
    offset() -> i32,
    pressed_position() -> i32,
    uniform_item_sizes() -> bool,
    column() -> i32,
    vertical_scroll_bar() -> &i32,
    horizontal_scroll_bar() -> &i32,
    vertical_scroll_mode() -> i32,
    horizontal_scroll_mode() -> i32,
    model_index(i32 row) -> i32,
    row_count() -> i32,
    view_options() -> i32,
    viewport() -> &i32,
    clip_rect() -> i32,
    cached_item_size() -> i32,
    view_item_rect(&i32 item) -> i32,
    item_size(&i32 opt, &i32 idx) -> i32,
    delegate(&i32 idx) -> &i32,
    is_hidden(i32 row) -> bool,
    hidden_count() -> i32,
    is_right_to_left() -> bool,
}

