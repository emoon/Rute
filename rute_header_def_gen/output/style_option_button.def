enum StyleOptionType {
    Type,
}

enum StyleOptionVersion {
    Version,
}

enum ButtonFeature {
    None,
    Flat,
    HasMenu,
    DefaultButton,
    AutoDefaultButton,
    CommandLinkButton,
}

