
struct SpanCollection {
    add_span(SpanCollection::Span * span),
    update_span(SpanCollection::Span * span, i32 old_height),
    span_at(i32 x, i32 y) -> SpanCollection::Span *,
    clear(),
    spans_in_rect(i32 x, i32 y, i32 w, i32 h) -> List<QSpanCollection::Span *>,
    update_inserted_rows(i32 start, i32 end),
    update_inserted_columns(i32 start, i32 end),
    update_removed_rows(i32 start, i32 end),
    update_removed_columns(i32 start, i32 end),
    clean_span_sub_index(&SubIndex subindex, i32 end, bool update) -> bool,
}

// vim: syntax=rust expandtab ts=4 sw=4
