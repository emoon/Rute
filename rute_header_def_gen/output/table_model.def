
struct TableModel {
    insert_rows(i32 row, i32 count, &i32 parent) -> bool,
    insert_columns(i32 column, i32 count, &i32 parent) -> bool,
    remove_rows(i32 row, i32 count, &i32 parent) -> bool,
    remove_columns(i32 column, i32 count, &i32 parent) -> bool,
    set_item(i32 row, i32 column, &i32 item),
    take_item(i32 row, i32 column) -> &i32,
    item(i32 row, i32 column) -> &i32,
    remove_item(&i32 item),
    set_horizontal_header_item(i32 section, &i32 item),
    set_vertical_header_item(i32 section, &i32 item),
    take_horizontal_header_item(i32 section) -> &i32,
    take_vertical_header_item(i32 section) -> &i32,
    horizontal_header_item(i32 section) -> &i32,
    vertical_header_item(i32 section) -> &i32,
    index(i32 row, i32 column, &i32 parent) -> i32,
    set_row_count(i32 rows),
    set_column_count(i32 columns),
    row_count(&i32 parent) -> i32,
    column_count(&i32 parent) -> i32,
    data(&i32 index, i32 role) -> i32,
    set_data(&i32 index, &i32 value, i32 role) -> bool,
    set_item_data(&i32 index, i32 arg0) -> bool,
    header_data(i32 section, i32 orientation, i32 role) -> i32,
    set_header_data(i32 section, i32 orientation, &i32 value, i32 role) -> bool,
    flags(&i32 index) -> i32,
    sort(i32 column, i32 order),
    item_less_than(i32 arg0) -> bool,
    item_greater_than(i32 arg0) -> bool,
    ensure_sorted(i32 column, i32 order, i32 start, i32 end),
    update_row_indexes(&i32 indexes, i32 moved_from_row, i32 moved_to_row),
    is_valid(&i32 index) -> bool,
    table_index(i32 row, i32 column) -> long,
    clear(),
    clear_contents(),
    item_changed(&i32 item),
    create_item() -> &i32,
    item_prototype() -> &i32,
    set_item_prototype(&i32 item),
    mime_types() -> i32,
    mime_data(&i32 indexes) -> &i32,
    drop_mime_data(&i32 data, i32 action, i32 row, i32 column, &i32 parent) -> bool,
    supported_drop_actions() -> i32,
    internal_mime_data() -> &i32,
}

// vim: syntax=rust expandtab ts=4 sw=4
