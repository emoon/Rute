enum Mode {
    Direct,
    Indexed,
    Gray,
}


struct Colormap {
    [static] initialize(),
    [static] cleanup(),
    [static] instance(screen: i32) -> ColormapType,
    mode() -> Colormap::Mode,
    depth() -> i32,
    size() -> i32,
    pixel(color: &ColorType) -> uint,
    color_at(pixel: uint) -> ColorType,
    colormap() -> [ColorType],
}

// vim: syntax=rust expandtab ts=4 sw=4
