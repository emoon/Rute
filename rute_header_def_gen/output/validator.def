[org_name(QValidator)]
enum State {
    Invalid,
    Intermediate,
    Acceptable,
}

struct Validator : Object {
    set_locale(locale: &LocaleType),
    locale() -> Locale,
    [replace] validate(arg0: String, arg1: &i32) -> Validator::State,
    [replace] fixup(arg0: String),
    [event] changed(),
}

// vim: syntax=rust expandtab ts=4 sw=4
