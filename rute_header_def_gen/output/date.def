
struct Date {
    is_null() -> bool,
    is_valid() -> bool,
    year() -> i32,
    month() -> i32,
    day() -> i32,
    day_of_week() -> i32,
    day_of_year() -> i32,
    days_in_month() -> i32,
    days_in_year() -> i32,
    week_number(&i32 year_num) -> i32,
    short_month_name(i32 month, Date::MonthNameType type) -> String,
    short_day_name(i32 weekday, Date::MonthNameType type) -> String,
    long_month_name(i32 month, Date::MonthNameType type) -> String,
    long_day_name(i32 weekday, Date::MonthNameType type) -> String,
    set_date(i32 year, i32 month, i32 day) -> bool,
    get_date(&i32 year, &i32 month, &i32 day),
    get_date(&i32 year, &i32 month, &i32 day),
    add_days(qint64 days) -> DateType,
    add_months(i32 months) -> DateType,
    add_years(i32 years) -> DateType,
    days_to(&DateType arg0) -> qint64,
    current_date() -> DateType,
    is_valid(i32 y, i32 m, i32 d) -> bool,
    is_leap_year(i32 year) -> bool,
    from_julian_day(qint64 jd) -> DateType,
    to_julian_day() -> qint64,
    null_jd() -> qint64,
    min_jd() -> qint64,
    max_jd() -> qint64,
}

