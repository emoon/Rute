// test

Rect {
    f32 x,
    f32 y,
    f32 width,
    f32 height,
}

Widget {
    show(),
    set_size(i32 width, i32 height),

	// events that can be overriden

    // [event] on_paint(Painter painter),
}

// Inherit functions from Button in PushButton

PushButton : Widget {
    [callback] released(),
    set_text(String text),
    set_flat(bool flat),
}

// Sliders

Slider : Widget {
    [callback] value_changed(i32 value),
}

Painter {
    draw_line(i32 x1, i32 y1, i32 x2, i32 y2),
}


